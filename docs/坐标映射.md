## 坐标映射
[坐标系统](https://learnopengl-cn.github.io/01%20Getting%20started/08%20Coordinate%20Systems/)  
![learn opengl](https://learnopengl-cn.github.io/img/01/08/coordinate_systems.png)
通常，在opengl里，我们的坐标计算牵扯到以下几个坐标矩阵

- projectionMatrix 投影矩阵
- viewMatrix 视图矩阵(观察矩阵)
- modelMatrix 模型矩阵
- mvpMatrix 模型-视图-投影矩阵

### 矩阵变换流程
```
局部空间 → 世界空间 → 相机空间 → 裁剪空间 → 屏幕空间
   ↓         ↓         ↓         ↓         ↓
modelMatrix viewMatrix projectionMatrix
   ↓         ↓         ↓
   └─────────┴─────────┘
        mvpMatrix
```

首先明确一点，
```
mvpMatrix = projectionMatrix * viewMatrix * modelMatrix  
```
其中，projectionMatrix/viewMatrix/modelMatrix的顺序不能变。

下面我们一一做解释
### 1. projectionMatrix 投影矩阵
将物体从相机空间变换到裁剪空间

#### 1.1 透视投影 vs 正交投影
```
透视投影 (Perspective Projection)
    /\
   /  \    近大远小效果
  /    \   模拟人眼视觉
 /      \
/        \

正交投影 (Orthographic Projection)
    ┌──┐
    │  │    无透视效果
    │  │   平行线保持平行
    │  │
    └──┘
```

有两种投影矩阵
- 透视投影，模拟近大远小，3D绘图通常使用这个
```
Matrix.perspectiveM(
    projectionMatrix, 0,
    radians(45.0f), // FOV
    screenWidth/screenHeight, // 宽高比
    0.1f, 100.0f // 近/远平面
);
```
- 正交投影, 无透视效果, 也就是远/近看到的效果一样，2D绘图通常使用这个
```
Matrix.orthoM(
    projectionMatrix, 0,
    -1.0f, 1.0f, // left/right
    -1.0f, 1.0f, // bottom/top
    0.1f, 100.0f // near/far
);
```
注意，这里的near/far都只能是正数

**参数详解：**
- **FOV (Field of View)**: 视野角度，通常45-60度，角度越大视野越宽但可能产生鱼眼效果
- **宽高比**: 屏幕宽度除以高度，影响物体的拉伸程度
- **近/远平面**: 定义可见范围，物体必须在近平面和远平面之间才能被渲染

### 2. viewMatrix 视图矩阵(观察矩阵)
将物体从世界空间变换到相机空间（观察空间），世界坐标 → 相机坐标,  
实际是定义相机的位置和方向（即"观察者"的视角）。
```
Matrix.setLookAtM(
    viewMatrix, 0,
    0f, 0f, 1f, // 相机位置
    0f, 0f, 0f, // 观察目标
    0f, 1f, 0f  // 上向量
)
```

注意，  
已知投影矩阵里，near/far的值，视图矩阵里相机位置(eye)的位置(eye_x, eye_y, eye_z),
那z的可见范围是[eye_z + near, eye_z + far], z超出这个范围的物体不会被显示。

**参数详解：**
- **相机位置 (eyeX, eyeY, eyeZ)**: 相机在世界空间中的位置
- **观察目标 (centerX, centerY, centerZ)**: 相机看向的点，决定相机的朝向
- **上向量 (upX, upY, upZ)**: 定义相机的"上"方向，通常是(0,1,0)

### 3. modelMatrix 模型矩阵
将物体从局部空间变换到世界空间，定义物体的位置、旋转、缩放等变换。

#### 3.1 基本变换类型
```
平移 (Translation)
    ┌─┐
    │ │     ┌─┐
    └─┘  →  │ │
            └─┘
    原点    移动后

旋转 (Rotation)
    ┌─┐
    │ │  →  ┌─┐
    └─┘     │ │
            └─┘
    原始    旋转后

缩放 (Scale)
    ┌─┐
    │ │  →  ┌─────┐
    └─┘     │     │
            └─────┘
    原始    放大后
```

**基本变换方法：**

#### 3.2 平移变换 (Translation)
```
Matrix.translateM(modelMatrix, 0, 2.0f, 0.0f, -3.0f);
// 参数：矩阵, 偏移量, x偏移, y偏移, z偏移
// 效果：将物体沿x轴移动2单位，沿z轴移动-3单位

平移效果示意图：
    Y
    ↑
    |    物体
    |   ↗
    | ↗
    +--------→ X
    原点

    平移后：
    Y
    ↑
    |     
    |     
    |     
    +--------→ X
    原点    物体
```

#### 3.3 旋转变换 (Rotation)
```
// 绕X轴旋转45度
Matrix.rotateM(modelMatrix, 0, 45.0f, 1.0f, 0.0f, 0.0f);

// 绕Y轴旋转90度  
Matrix.rotateM(modelMatrix, 0, 90.0f, 0.0f, 1.0f, 0.0f);

// 绕Z轴旋转180度
Matrix.rotateM(modelMatrix, 0, 180.0f, 0.0f, 0.0f, 1.0f);

旋转轴示意图：
    Y (上轴)
    ↑
    |     Z (前轴)
    |   ↗
    | ↗
    +--------→ X (右轴)

绕X轴旋转：
    Y
    ↑
    |    物体绕X轴旋转
    |   ↗
    | ↗
    +--------→ X

绕Y轴旋转：
    Y
    ↑
    |     
    |     物体绕Y轴旋转
    |   ↗
    +--------→ X

绕Z轴旋转：
    Y
    ↑
    |     
    |     
    |     物体绕Z轴旋转
    +--------→ X
```
**参数详解：**
- **角度**: 旋转角度（度），正值逆时针旋转
- **轴向量**: (x, y, z) 表示旋转轴的方向向量

#### 3.4 缩放变换 (Scale)
```
Matrix.scaleM(modelMatrix, 0, 2.0f, 1.5f, 0.5f);
// 参数：矩阵, 偏移量, x缩放, y缩放, z缩放
// 效果：x方向放大2倍，y方向放大1.5倍，z方向缩小到0.5倍

缩放效果示意图：
原始物体：
    ┌─┐
    │ │
    └─┘

X方向放大2倍：
    ┌─────┐
    │     │
    └─────┘

Y方向放大1.5倍：
    ┌─┐
    │ │
    │ │
    │ │
    └─┘

Z方向缩小0.5倍：
    ┌─┐
    └─┘
```

#### 3.5 组合变换
```
// 注意：变换的顺序很重要！通常是 缩放 -> 旋转 -> 平移
Matrix.setIdentityM(modelMatrix, 0);  // 重置为单位矩阵
Matrix.scaleM(modelMatrix, 0, 2.0f, 2.0f, 2.0f);  // 先缩放
Matrix.rotateM(modelMatrix, 0, 45.0f, 0.0f, 1.0f, 0.0f);  // 再旋转
Matrix.translateM(modelMatrix, 0, 1.0f, 0.0f, 0.0f);  // 最后平移

变换顺序的重要性：
错误顺序：平移 -> 旋转 -> 缩放
    ┌─┐
    │ │  →  移动  →  旋转  →  缩放
    └─┘

正确顺序：缩放 -> 旋转 -> 平移
    ┌─┐
    │ │  →  缩放  →  旋转  →  移动
    └─┘
```

### 4. mvpMatrix 模型-视图-投影矩阵
这是最终的变换矩阵，将顶点从局部空间直接变换到裁剪空间。

#### 4.1 矩阵乘法流程
```
modelMatrix × viewMatrix × projectionMatrix = mvpMatrix

┌─┐   ┌─┐   ┌─┐     ┌─┐
│M│ × │V│ × │P│  =  │MVP│
└─┘   └─┘   └─┘     └─┘

计算步骤：
1. temp = M × V
2. MVP = P × temp
```

```
// 计算MVP矩阵
Matrix.multiplyMM(mvpMatrix, 0, viewMatrix, 0, modelMatrix, 0);
Matrix.multiplyMM(mvpMatrix, 0, projectionMatrix, 0, mvpMatrix, 0);
```

**参数详解：**
- **result**: 存储结果的矩阵
- **resultOffset**: 结果矩阵的偏移量
- **lhs**: 左操作数矩阵
- **lhsOffset**: 左操作数矩阵的偏移量
- **rhs**: 右操作数矩阵
- **rhsOffset**: 右操作数矩阵的偏移量

### 5. 完整代码示例

```java
public class CoordinateSystemExample {
    private float[] projectionMatrix = new float[16];
    private float[] viewMatrix = new float[16];
    private float[] modelMatrix = new float[16];
    private float[] mvpMatrix = new float[16];
    
    public void setupMatrices(int screenWidth, int screenHeight) {
        // 1. 设置投影矩阵 - 透视投影
        float aspectRatio = (float) screenWidth / screenHeight;
        Matrix.perspectiveM(projectionMatrix, 0, 45.0f, aspectRatio, 0.1f, 100.0f);
        
        // 2. 设置视图矩阵 - 相机位置和朝向
        Matrix.setLookAtM(viewMatrix, 0,
            0.0f, 0.0f, 3.0f,  // 相机位置：在z轴正方向3单位处
            0.0f, 0.0f, 0.0f,  // 观察目标：原点
            0.0f, 1.0f, 0.0f   // 上向量：y轴正方向
        );
        
        // 3. 设置模型矩阵 - 物体变换
        Matrix.setIdentityM(modelMatrix, 0);  // 重置为单位矩阵
        Matrix.translateM(modelMatrix, 0, 0.0f, 0.0f, -5.0f);  // 移动到z轴负方向
        Matrix.rotateM(modelMatrix, 0, 45.0f, 0.0f, 1.0f, 0.0f);  // 绕Y轴旋转45度
        
        // 4. 计算MVP矩阵
        Matrix.multiplyMM(mvpMatrix, 0, viewMatrix, 0, modelMatrix, 0);
        Matrix.multiplyMM(mvpMatrix, 0, projectionMatrix, 0, mvpMatrix, 0);
    }
    
    public void render() {
        // 将MVP矩阵传递给着色器
        GLES20.glUniformMatrix4fv(mvpMatrixHandle, 1, false, mvpMatrix, 0);
        // 绘制物体...
    }
}
```

### 6. 实际应用场景

#### 6.1 第一人称相机
```
相机移动示意图：
    Y
    ↑
    |    相机
    |   ↗
    | ↗
    +--------→ X
    观察目标

鼠标移动后：
    Y
    ↑
    |     
    |     相机
    |   ↗
    +--------→ X
    观察目标
```

```java
// 相机跟随鼠标移动
public void updateFirstPersonCamera(float mouseX, float mouseY) {
    // 计算相机位置（假设在原点）
    float cameraX = 0.0f;
    float cameraY = 0.0f;
    float cameraZ = 0.0f;
    
    // 计算观察目标（基于鼠标位置）
    float targetX = (float) Math.sin(mouseX) * 5.0f;
    float targetZ = (float) Math.cos(mouseX) * 5.0f;
    float targetY = mouseY * 2.0f;
    
    Matrix.setLookAtM(viewMatrix, 0,
        cameraX, cameraY, cameraZ,  // 相机位置
        targetX, targetY, targetZ,  // 观察目标
        0.0f, 1.0f, 0.0f           // 上向量
    );
}
```

#### 6.2 物体动画
```
旋转动画示意图：
帧1:   帧2:   帧3:   帧4:
┌─┐    ┌─┐    ┌─┐    ┌─┐
│ │    │ │    │ │    │ │
└─┘    └─┘    └─┘    └─┘
 0°     45°    90°    135°
```

```java
// 物体旋转动画
public void animateObject(float deltaTime) {
    static float rotationAngle = 0.0f;
    rotationAngle += deltaTime * 50.0f;  // 每秒旋转50度
    
    Matrix.setIdentityM(modelMatrix, 0);
    Matrix.translateM(modelMatrix, 0, 2.0f, 0.0f, 0.0f);  // 移动到右侧
    Matrix.rotateM(modelMatrix, 0, rotationAngle, 0.0f, 1.0f, 0.0f);  // 绕Y轴旋转
}
```

#### 6.3 多物体渲染
```
多物体排列示意图：
物体1  物体2  物体3  物体4
┌─┐   ┌─┐   ┌─┐   ┌─┐
│ │   │ │   │ │   │ │
└─┘   └─┘   └─┘   └─┘
 0°    30°   60°   90°
```

```java
// 渲染多个物体，每个物体有不同的模型矩阵
public void renderMultipleObjects() {
    for (int i = 0; i < objectCount; i++) {
        // 为每个物体创建独立的模型矩阵
        Matrix.setIdentityM(modelMatrix, 0);
        Matrix.translateM(modelMatrix, 0, i * 2.0f, 0.0f, 0.0f);  // 水平排列
        Matrix.rotateM(modelMatrix, 0, i * 30.0f, 0.0f, 1.0f, 0.0f);  // 不同角度
        
        // 计算当前物体的MVP矩阵
        Matrix.multiplyMM(mvpMatrix, 0, viewMatrix, 0, modelMatrix, 0);
        Matrix.multiplyMM(mvpMatrix, 0, projectionMatrix, 0, mvpMatrix, 0);
        
        // 渲染物体
        GLES20.glUniformMatrix4fv(mvpMatrixHandle, 1, false, mvpMatrix, 0);
        drawObject();
    }
}
```

### 7. 常见问题和调试技巧

#### 7.1 物体不可见
```
可见性检查示意图：
    Y
    ↑
    |    远平面 (100)
    |   ↗
    | ↗
    +--------→ X
    近平面 (0.1)

物体必须在近平面和远平面之间才能被渲染
```

- 检查物体是否在近/远平面范围内
- 确认相机位置和观察目标设置正确
- 验证模型矩阵的变换是否合理

#### 7.2 物体变形
```
变形问题示意图：
正常比例：    宽高比错误：
┌─────┐      ┌─────────────┐
│     │      │             │
│     │      │             │
└─────┘      └─────────────┘
```

- 检查宽高比设置是否正确
- 确认FOV角度是否合适
- 验证缩放参数是否合理

#### 7.3 性能优化
```
优化策略示意图：
静态物体：    动态物体：
┌─┐          ┌─┐
│ │ 预计算   │ │ 每帧计算
└─┘ MVP矩阵  └─┘ MVP矩阵
```

- 对于静态物体，预计算MVP矩阵
- 使用矩阵池避免频繁创建新矩阵
- 批量渲染相同材质的物体

### 8. 调试工具和技巧

#### 8.1 矩阵可视化
```java
// 打印矩阵内容进行调试
public void printMatrix(String name, float[] matrix) {
    System.out.println(name + ":");
    for (int i = 0; i < 4; i++) {
        for (int j = 0; j < 4; j++) {
            System.out.printf("%8.3f ", matrix[i * 4 + j]);
        }
        System.out.println();
    }
    System.out.println();
}
```

#### 8.2 坐标轴可视化
```
调试用的坐标轴：
    Y (绿色)
    ↑
    |     Z (蓝色)
    |   ↗
    | ↗
    +--------→ X (红色)
```

通过理解这些矩阵的作用和参数，你就能更好地控制OpenGL中的3D场景渲染了！

### 矩阵参数间的配合与影响关系

#### 投影矩阵参数对后续矩阵的影响

投影矩阵的参数设置会直接影响viewMatrix和modelMatrix中参数的选择。下面以屏幕大小500×350为例，详细说明不同投影参数的影响：

##### 1. 正交投影参数对比

**情况A：标准化坐标 (-1,1)**
```java
// 投影矩阵：使用标准化坐标
Matrix.orthoM(projectionMatrix, 0,
    -1.0f, 1.0f,    // left/right
    -1.0f, 1.0f,    // bottom/top  
    0.1f, 100.0f    // near/far
);
```

**情况B：像素坐标 (-250,250)**
```java
// 投影矩阵：使用像素坐标
Matrix.orthoM(projectionMatrix, 0,
    -250.0f, 250.0f,  // left/right
    -175.0f, 175.0f,  // bottom/top
    0.1f, 100.0f      // near/far
);
```

**情况C：正方形坐标 (-250,250)**
```java
// 投影矩阵：使用正方形坐标
Matrix.orthoM(projectionMatrix, 0,
    -250.0f, 250.0f,  // left/right
    -250.0f, 250.0f,  // bottom/top
    0.1f, 100.0f      // near/far
);
```

##### 2. 参数影响分析

```
标准化坐标 (-1,1) 的影响：
┌─────────────────────────────────────┐
│ 裁剪空间范围: (-1,-1) 到 (1,1)      │
│ 屏幕映射: 500×350 像素              │
│ 坐标单位: 1单位 = 250像素(宽)/175像素(高) │
│ 宽高比: 1:1 (正方形)                │
└─────────────────────────────────────┘

像素坐标 (-250,250) 的影响：
┌─────────────────────────────────────┐
│ 裁剪空间范围: (-250,-175) 到 (250,175) │
│ 屏幕映射: 500×350 像素              │
│ 坐标单位: 1单位 = 1像素              │
│ 宽高比: 500:350 ≈ 1.43:1            │
└─────────────────────────────────────┘

正方形坐标 (-250,250) 的影响：
┌─────────────────────────────────────┐
│ 裁剪空间范围: (-250,-250) 到 (250,250) │
│ 屏幕映射: 500×350 像素              │
│ 坐标单位: 1单位 = 1像素(宽)/0.7像素(高) │
│ 宽高比: 1:1 (正方形)                │
└─────────────────────────────────────┘
```

##### 3. 三种情况的对比分析

**坐标系统对比：**
```
情况A (标准化坐标):
    Y
    ↑
    |     Z
    |   ↗
    | ↗
    +--------→ X
    (-1,-1) 到 (1,1)

情况B (像素坐标):
    Y
    ↑
    |     Z
    |   ↗
    | ↗
    +--------→ X
    (-250,-175) 到 (250,175)

情况C (正方形坐标):
    Y
    ↑
    |     Z
    |   ↗
    | ↗
    +--------→ X
    (-250,-250) 到 (250,250)
```

**实际渲染效果对比：**

```
情况A：标准化坐标
屏幕: 500×350
┌─────────────────────────────────┐
│ 坐标范围: (-1,-1) 到 (1,1)      │
│ 实际像素: 500×350               │
│ 单位换算: 1单位 = 250×175像素    │
│ 特点: 保持比例，适合3D场景       │
└─────────────────────────────────┘

情况B：像素坐标
屏幕: 500×350
┌─────────────────────────────────┐
│ 坐标范围: (-250,-175) 到 (250,175) │
│ 实际像素: 500×350               │
│ 单位换算: 1单位 = 1像素          │
│ 特点: 像素完美，适合2D UI        │
└─────────────────────────────────┘

情况C：正方形坐标
屏幕: 500×350
┌─────────────────────────────────┐
│ 坐标范围: (-250,-250) 到 (250,250) │
│ 实际像素: 500×350               │
│ 单位换算: 1单位 = 1×0.7像素      │
│ 特点: 正方形映射，Y轴压缩        │
└─────────────────────────────────┘
```

##### 4. 对viewMatrix的影响

**情况A：标准化坐标下的viewMatrix设置**
```java
// 由于投影矩阵使用标准化坐标，viewMatrix需要考虑坐标单位
Matrix.setLookAtM(viewMatrix, 0,
    0.0f, 0.0f, 2.0f,    // 相机位置：距离原点2个单位
    0.0f, 0.0f, 0.0f,    // 观察目标：原点
    0.0f, 1.0f, 0.0f     // 上向量
);

// 可见范围：z在[-1.9, 102]之间（相机z=2.0，near=0.1, far=100）
```

**情况B：像素坐标下的viewMatrix设置**
```java
// 由于投影矩阵使用像素坐标，viewMatrix需要使用更大的数值
Matrix.setLookAtM(viewMatrix, 0,
    0.0f, 0.0f, 500.0f,  // 相机位置：距离原点500像素
    0.0f, 0.0f, 0.0f,    // 观察目标：原点
    0.0f, 1.0f, 0.0f     // 上向量
);

// 可见范围：z在[-499.9, 600]之间（相机z=500，near=0.1, far=100）
```

**情况C：正方形坐标下的viewMatrix设置**
```java
// 由于投影矩阵使用正方形坐标，viewMatrix需要考虑Y轴压缩
Matrix.setLookAtM(viewMatrix, 0,
    0.0f, 0.0f, 500.0f,  // 相机位置：距离原点500像素
    0.0f, 0.0f, 0.0f,    // 观察目标：原点
    0.0f, 1.0f, 0.0f     // 上向量
);

// 可见范围：z在[-499.9, 600]之间（相机z=500，near=0.1, far=100）
// 注意：Y轴会被压缩，因为屏幕高度350 < 坐标范围500
```

##### 5. 对modelMatrix的影响

**情况A：标准化坐标下的modelMatrix设置**
```java
// 物体大小和位置需要考虑标准化坐标
Matrix.setIdentityM(modelMatrix, 0);
Matrix.translateM(modelMatrix, 0, 0.5f, 0.3f, -1.0f);  // 移动0.5单位右，0.3单位上
Matrix.scaleM(modelMatrix, 0, 0.2f, 0.2f, 0.2f);       // 缩放为原来的0.2倍

// 实际屏幕位置：x=125像素，y=52.5像素
// 实际屏幕大小：100×70像素
```

**情况B：像素坐标下的modelMatrix设置**
```java
// 物体大小和位置直接使用像素值
Matrix.setIdentityM(modelMatrix, 0);
Matrix.translateM(modelMatrix, 0, 125.0f, 52.5f, -100.0f);  // 直接移动到像素位置
Matrix.scaleM(modelMatrix, 0, 50.0f, 35.0f, 1.0f);          // 直接设置像素大小

// 实际屏幕位置：x=125像素，y=52.5像素
// 实际屏幕大小：100×70像素
```

**情况C：正方形坐标下的modelMatrix设置**
```java
// 物体大小和位置需要考虑Y轴压缩
Matrix.setIdentityM(modelMatrix, 0);
Matrix.translateM(modelMatrix, 0, 125.0f, 52.5f, -100.0f);  // 移动到像素位置
Matrix.scaleM(modelMatrix, 0, 50.0f, 35.0f, 1.0f);          // 设置像素大小

// 实际屏幕位置：x=125像素，y=52.5像素
// 实际屏幕大小：100×70像素
// 注意：Y轴会被压缩，圆形会变成椭圆
```

##### 6. 三种情况的实际效果对比

**渲染一个100×100的正方形纹理：**

```
情况A (标准化坐标):
┌─────────────────────────────────┐
│ 坐标: (-0.1, -0.1) 到 (0.1, 0.1) │
│ 屏幕: 25×17.5像素                │
│ 效果: 保持正方形比例             │
└─────────────────────────────────┘

情况B (像素坐标):
┌─────────────────────────────────┐
│ 坐标: (-50, -50) 到 (50, 50)    │
│ 屏幕: 100×100像素               │
│ 效果: 像素完美，保持正方形       │
└─────────────────────────────────┘

情况C (正方形坐标):
┌─────────────────────────────────┐
│ 坐标: (-50, -50) 到 (50, 50)    │
│ 屏幕: 100×70像素                │
│ 效果: Y轴压缩，正方形变矩形      │
└─────────────────────────────────┘
```

**渲染一个圆形纹理：**

```
情况A (标准化坐标):
┌─────────────────────────────────┐
│ 圆形保持圆形                    │
│ 适合3D场景                      │
└─────────────────────────────────┘

情况B (像素坐标):
┌─────────────────────────────────┐
│ 圆形保持圆形                    │
│ 适合2D UI                       │
└─────────────────────────────────┘

情况C (正方形坐标):
┌─────────────────────────────────┐
│ 圆形变成椭圆                    │
│ Y轴被压缩                       │
└─────────────────────────────────┘
```

##### 7. 使用场景建议

**情况A (标准化坐标) - 推荐用于：**
- 3D场景渲染
- 需要保持几何比例的应用
- 跨平台游戏开发
- 科学可视化

**情况B (像素坐标) - 推荐用于：**
- 2D UI界面
- 像素艺术游戏
- 精确像素控制
- 移动应用界面

**情况C (正方形坐标) - 不推荐使用：**
- 会导致Y轴压缩
- 圆形变成椭圆
- 几何变形
- 仅适用于特殊需求（如等距投影）

##### 8. 最佳实践总结

**选择投影矩阵的原则：**

1. **保持几何比例** → 选择情况A或情况B
2. **像素完美控制** → 选择情况B
3. **3D场景渲染** → 选择情况A
4. **2D UI开发** → 选择情况B
5. **避免几何变形** → 避免情况C

**实际应用建议：**
```java
// 2D UI开发（推荐）
Matrix.orthoM(projectionMatrix, 0, -screenWidth/2, screenWidth/2, -screenHeight/2, screenHeight/2, 0.1f, 100.0f);

// 3D场景开发（推荐）
Matrix.orthoM(projectionMatrix, 0, -1.0f, 1.0f, -1.0f, 1.0f, 0.1f, 100.0f);

// 避免使用（会导致变形）
Matrix.orthoM(projectionMatrix, 0, -250.0f, 250.0f, -250.0f, 250.0f, 0.1f, 100.0f);
```
